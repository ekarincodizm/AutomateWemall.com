*** Variables ***

# ${DB_HOSTNAME}      192.168.99.100
# ${DB_USERNAME}      root
# ${DB_PASSWORD}      root
# ${DB_NAME}          pcms_db
# ${DB_CHARSET}       utf8

${DB_HOSTNAME}      127.0.0.1
${DB_USERNAME}      root
${DB_PASSWORD}
${DB_NAME}          pcms_db
${DB_CHARSET}       utf8

${POINT_DB_HOSTNAME}      127.0.0.1
${POINT_DB_USERNAME}      root
${POINT_DB_PASSWORD}
${POINT_DB_NAME}          point_db
${POINT_DB_CHARSET}       utf8